# Email Configuration for Contact Form
# You need to add these to your .env file in the server directory

# Gmail Configuration Example:
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password  # Generate app password from Google Account settings
RECEIVER_EMAIL=admin@rooted.com  # Email address where contact form submissions will be sent

# To generate a Gmail App Password:
# 1. Go to your Google Account: https://myaccount.google.com/
# 2. Select Security
# 3. Under "Signing in to Google," select 2-Step Verification (must be enabled)
# 4. At the bottom, select App passwords
# 5. Select "Mail" and your device, then Generate
# 6. Use the 16-character password generated

# For other email providers (e.g., SendGrid, Mailgun, Outlook):
# Update the transporter configuration in contact-controller.js accordingly
